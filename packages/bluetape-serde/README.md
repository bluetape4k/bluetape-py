# bluetape-serde

`bluetape-serde` currently establishes the source-workspace package boundary for strict
payload contracts and JSON serialization. PyPI publication remains on hold until the public
behavior is implemented and release-approved.
