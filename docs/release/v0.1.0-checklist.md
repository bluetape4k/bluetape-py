# v0.1.0 Release Checklist

Date: 2026-07-10 KST
Target version: `v0.1.0`
Release authority: repository owner `debop`
Consumer scope: 첫 public Python-native foundation release

## 대상 상태

- Source branch: `develop`
- Release branch: `main`
- Tag: `v0.1.0`
- GitHub Release: `v0.1.0`
- PyPI publish: trusted publishing을 확인할 때까지 HOLD

## 현재 근거

- Milestone `0.1.0` open issue: `0`
- Open PR: `0`
- Local `develop`: clean이며 `origin/develop`과 동일
- Local 및 remote `v0.1.0` tag: 없음
- GitHub Release `v0.1.0`: 없음
- 최신 `develop` CI: `c8c8a348e0e3cee364b9bcbbf02f6ff8b418f5e8`에서 통과

## 필수 로컬 게이트

- `uv sync --all-packages`
- `uv run pytest`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv build --all-packages`
- `actionlint`
- `git diff --check`

## Release 순서

1. Release-prep 변경을 `develop`에 merge합니다.
2. Milestone `0.1.0`을 닫습니다.
3. `develop -> main` release PR을 열고 merge합니다.
4. annotated tag `v0.1.0`을 `main`에 붙입니다.
5. GitHub Release `v0.1.0`을 만듭니다.
6. Trusted publishing을 확인할 때까지 PyPI publication을 차단합니다.

## Publish hold

PyPI publication은 이 checklist에서 의도적으로 제외합니다. package를 upload하기 전 다음 distribution의 ownership/trusted publishing을 확인합니다.

- `bluetape`
- `bluetape-core`
- `bluetape-logging`
- `bluetape-testing`
